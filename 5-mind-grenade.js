const num1 = 5;
const num2 = 10;

const addValues = () => {
  console.log(`the sum is: ${num1 + num2}`)
}

addValues()

// when node exports this file, it wraps the whole into the function that is executed on line 8